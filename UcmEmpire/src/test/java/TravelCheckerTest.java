import controllers.TravelChecker;
import models.Character;
import models.buildings.Forum;
import models.units.Farmer;
import org.junit.Assert;
import org.junit.Test;

import static org.junit.Assert.*;

public class TravelCheckerTest {

    /*@Test //TODO : BRYAN
    public void movableEntity_Character_20pa_true() {
        assertTrue(TravelChecker.movableEntity(new Character(20, "test", 20)));
    }

    @Test
    public void movableEntity_Farmer_20pa_true() {
        assertTrue(TravelChecker.movableEntity(new Farmer(20, "test", 20)));
    }

    @Test
    public void movableEntity_Character_0pa_false() {
        assertFalse(TravelChecker.movableEntity(new Character(20, "test", 0)));
    }

    @Test
    public void movableEntity_Forum_0pa_false() {
        assertFalse(TravelChecker.movableEntity(new Forum(20, "test", null)));
    }*/
}